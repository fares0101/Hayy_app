import 'package:flutter/material.dart';

class RatingStars extends StatelessWidget {
  const RatingStars({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(); // TODO: Implement rating stars
  }
}

class ReviewTextField extends StatelessWidget {
  const ReviewTextField({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(); // TODO: Implement review text field
  }
}

class SubmitReviewButton extends StatelessWidget {
  const SubmitReviewButton({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(); // TODO: Implement submit review button
  }
}