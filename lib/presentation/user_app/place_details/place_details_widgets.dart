import 'package:flutter/material.dart';

class PlaceImageGallery extends StatelessWidget {
  const PlaceImageGallery({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(); // TODO: Implement place image gallery
  }
}

class PlaceInfoCard extends StatelessWidget {
  const PlaceInfoCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(); // TODO: Implement place info card
  }
}

class ReviewsList extends StatelessWidget {
  const ReviewsList({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(); // TODO: Implement reviews list
  }
}